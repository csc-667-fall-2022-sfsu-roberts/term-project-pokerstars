extends layout

block content
  h1 Login page
  p Welcome to #{PokerStars}

  form(action="/login" method="POST")
    div
      label(for="username")
      input(type="text" name="username")

    div
      label(for="password")
      input(type="password" name="password")

    div
      input(type="submit" value="Login")